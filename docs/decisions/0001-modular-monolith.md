# 0001: Modular monolith

Status: Accepted

The first version uses a modular monolith. Package boundaries are represented by
source directories, public exports, tests, and documentation before any separate
deployment or package publishing is introduced.

The core learning protocol remains independent from React, browser storage,
subject packages, and future AI providers.
